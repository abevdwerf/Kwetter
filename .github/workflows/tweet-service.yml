# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
    # paths:
    #  - 'tweet-service/**'
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file tweet-service/pom.xml

  docker:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
    - name: checkout
      uses: actions/checkout@v3
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: "tweet-service"
        file: ./tweet-service/Dockerfile
        push: true
        tags: ${{ secrets.DOCKERHUB_USERNAME }}/kwetter-tweet-service:latest
    # - name: build
    #   run: |
    #     docker build -f tweet-service/Dockerfile -t abevdwerf/kwetter-tweet-service:latest tweet-service
    # - name: publish
    #   run: |
    #     docker login -u abevdwerf -p ${{ secrets.DOCKER_PASSWORD }}
    #     docker push abevdwerf/kwetter-tweetService:latest
